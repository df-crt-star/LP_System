# CLAUDE.md — LP自動コーディングシステム 指揮官ファイル

> **このファイルはAI（Claude Code）への行動指示書である。コーディングルールはゼロ。「いつ・何を読むか」と「どう動くか」だけを定義する。**

---

## 1. 読み込み戦略（必ず守ること）

ファイルを読み飛ばして自己判断でコーディングすることを固く禁ずる。

### 1.1 【常時読み込み】コアルール（起動時に必ず読む）

プロジェクト起動時、**必ず最初に**以下のファイルを読み込み、セッションが終了するまで記憶し続けること。

- `core/rules-core.md` — 絶対ルール（128行・CSS変数定義・禁止事項）

コード例の参照が必要になった場合のみ、追加で読み込む：

- `core/rules-reference.md` — コード例置き場（必要時のみ）

### 1.2 【案件設定時読み込み】コンポーネントルール（STEP 0で選択されたものだけ読む）

STEP 0で選択されたコンポーネントのファイルのみ読み込む。選択されなかったファイルは読まない。

| コンポーネント | 読み込むファイル |
|--------------|----------------|
| アニメーション（A1〜A8） | `components/animation.md` |
| フォーム・アコーディオンCTA | `components/form.md` |
| 追従CTAバー | `components/sticky.md` |
| ユーザーボイス・お客様の声 | `components/voice.md` |
| アコーディオンFAQ | `components/faq.md` |
| 各種セクションテンプレート | `components/section-templates.md` |
| スライダー（jQuery案件） | `components/slider.md` |
| ポップアップ | `components/popup.md` |

---

## 2. 対話型カスタムビルドフロー（絶対遵守の7ステップ）

AIは以下の7ステップを**いかなる例外もなく**遵守すること。人間からの明示的な承認がない限り、勝手に次のステップへ進んではならない。

---

### STEP 0：案件設定（コンポーネント・アニメーション選択）

作業開始依頼を受けた際、**コードを一行も書く前に**、以下を提示して選択を待つこと。

```text
LP制作を開始します。使用するコンポーネントを選択してください。

【アニメーション】（複数選択可）
[A1] フェードイン（スクロール連動）
[A2] ふわふわ浮遊
[A3] 揺れ（左右シェイク）
[A4] 脈打ち（拡縮パルス）
[A5] カウントアップ
[A6] スライドイン（左から）
[A7] スライドイン（右から）
[A8] ズームイン
[A9] カスタムアニメーション（参考URLまたは言葉で指定）
[A0] アニメーションなし

【コンポーネント】（複数選択可）
[C1] フォーム・アコーディオンCTA
[C2] 追従CTAバー
[C3] ユーザーボイス・お客様の声
[C4] アコーディオンFAQ
[C0] 上記なし

選択が完了したら、選択されたコンポーネントファイルを読み込み、STEP 0.5へ進みます。
```

---

### STEP 0.5：構成案・デザインカンプの読み込み

```text
構成案の種類を教えてください。

[1] Googleスプレッドシート（gws CLIで「全体構成」シートを取得します）
[2] Figma（構成案として読み込みます）
[3] 画像・PDF（マルチモーダル解析します）
[4] なし（STEP 1へ進みます）
```

構成案を読み込んだ後、`design/` フォルダが存在する場合：
- `01_〜.jpg` を番号順に読み込み、セクション構成を把握する
- 「以下の構成を確認しました：[セクション一覧]」と提示して人間に確認を取る

---

### STEP 1：ヒアリング（各コンポーネントの「必須」項目のみ確認。「任意」はデフォルト値で進める）

各コンポーネントファイルの確認事項には「必須」「任意」の区分がある。**「必須」項目のみ会話で確認し、「任意」項目はデフォルト値のまま進めること。**ユーザーから指定があった場合のみ変更する。

構成案・カンプで確認できた項目はスキップし、**未確定の項目のみ**質問する。

確認項目（構成案がある場合はほぼスキップ）：

```text
以下の未確定項目を確認します。

1. PC版の最大横幅（例：1200px / 1000px）
2. 基本フォント・メインカラー・アクセントカラー
   （例：Noto Sans JP / メイン #333333 / アクセント #e74c3c）
3. その他、全体を通して適用したい独自ルール（なければ「なし」）
```

回答が揃ったら、`core/rules-core.md` の CSS変数定義（`--max-width`, `--color-main` 等）に値を当てはめて確認を取る。

---

### STEP 2：ローカル素材のスキャン

STEP 1の確認が完了したら、案件フォルダ内の `images/` および `videos/` ディレクトリを自動スキャンし、格納されているファイル（WebP・MP4・GIF）の一覧を提示すること。

---

### STEP 3：コーディング（カンプを参照しながらセクションごとに実装）

各セクションのコーディングに入る前に、`design/` フォルダの該当カンプ（例：`01_fv.jpg`）を再参照し、「このカンプを元に実装します」と提示してからコードを生成すること。

`design/` フォルダが存在しない場合は、セクション名を提示して確認を取ってからコーディングを開始する。

---

### STEP 4：カスタム指示（追加・修正の受け付け）

人間から追加指示・修正依頼を受けた場合：

- 指示を **100%** 再現することを最優先とする
- 指示に曖昧な点がある場合は、コード生成前に必ず追加質問を行い仕様を確定させる
- 自己解釈で進めることを禁ずる

---

### STEP 5：確認（1セクションごとの確定ルール）

1つのセクションのコーディングが完了したら、必ずそこで処理を止め、人間にブラウザでのプレビュー確認を促すこと。

人間から「OK」「次へ」などの**明示的な承認を受け取るまで、絶対に次のセクションのコーディングを開始してはならない。**

---

## 3. 運用ルールの分離について

- 人間側の運用フロー、Googleドライブ連携、フォルダ命名規則などについては、人間向けマニュアルである `system-requirements.md` を参照すること（AIがこれを読んで自律行動する必要はない）
- AIは、`core/rules-core.md` に記載されたコーディングルールを、上記の対話フローの中で100%忠実に実行する「実務担当者」として振る舞うこと
