# CLAUDE.md — LP_System プロジェクト指示書（指揮官ファイル）

> **【最重要】**
> 本ファイルは、AI（Claude Code）がこのプロジェクトで作業する際の「絶対的な行動規範」である。
> AIはプロジェクトを開いた瞬間にこのファイルを最初に読み込み、以下のすべての指示に従って行動すること。
> 本ファイルには「行動命令」のみを記述する。具体的なコーディングルールは指定された別ファイルを参照すること。

---

## 1. ルールファイルの読み込み戦略

AIは以下の2段階でルールファイルを読み込む。指定されたファイルを読み飛ばして自己判断でコーディングすることを固く禁ずる。

### 1.1 【常時読み込み】コアルール（起動時に必ず全て読む）
プロジェクト起動時、**必ず最初に**以下のファイルを読み込み、セッションが終了するまで記憶し続けること。

- `our-standard-rules.md` — 制作部共通コーディングルール・ガイドライン全文

### 1.2 【作業時読み込み】コンポーネントルール（該当セクション制作直前に読む）
コーディング作業中、以下の要素を含むセクションを制作する直前に、該当するファイルを読み込んでからコードを生成すること。

| 制作内容 | 読み込むファイル |
|---------|----------------|
| アニメーション演出を含む箇所 | `components/animation.md` |
| フォーム（入力・ステップ）を含む箇所 | `components/form.md` |
| 追従要素（ヘッダー・CTA等）を含む箇所 | `components/sticky.md` |
| 各種セクション（FV、特徴、料金等） | `components/section-templates.md` |

*(※注: `components/` 以下のファイルは順次整備予定。ファイルが存在しない場合はスキップしてよい)*

---

## 2. 対話型カスタムビルドフロー（絶対遵守の5ステップ）

AIは以下の5ステップを**いかなる例外もなく**遵守すること。人間からの指示がない限り、勝手に次のステップへ進んではならない。

### STEP 1: 起動時の初期ヒアリング
作業開始依頼を受けた際、**コードを一行も書く前に**、必ず以下のヒアリング項目を人間に提示し、回答を待つこと。すべての回答が揃うまで作業を開始してはならない。

```text
LP制作を開始します。以下の4項目をすべてご回答ください。
確認が取れ次第、コーディングを開始します。

1. PC版の最大横幅（例：1200px / 1000px）
2. 基本フォント・メインカラー・アクセントカラー
   （例：Noto Sans JP / メイン #333333 / アクセント #e74c3c）
3. 使用予定の外部ライブラリ（例：AOS.js, Swiper.js ／ なければ「なし」）
4. その他、全体を通して適用したい独自ルール（自由記述。なければ「なし」）
```

### STEP 2: ローカル素材のスキャン
STEP 1の回答が揃ったら、AIは指定された案件フォルダ内の `images/` および `videos/` ディレクトリを自動スキャンし、格納されているファイル（WebP・MP4・GIF）の一覧を人間に提示すること。

### STEP 3: セクション制作前の「3案提示」
各セクションのコーディングに入る前に、AIは必ず以下の対話テンプレートを出力し、人間に確認を取ること。

```text
-----------------------------------------
次は「[セクション名]」の制作です。
以下の選択肢からベース仕様をお選びいただくか、[4]で自由にカスタム指示をしてください。

[1] フォーマットA：[components/section-templates.md で定義されたA案の概要]
[2] フォーマットB：[components/section-templates.md で定義されたB案の概要]
[3] フォーマットC：[components/section-templates.md で定義されたC案の概要]
[4] 自由カスタム（テキスト入力）：
    一部変更や完全オリジナルの仕様を言葉で自由に指示してください。
-----------------------------------------
```

### STEP 4: 自由カスタム指示の絶対最優先
人間が `[4]` を選択、またはテキストで独自の要望を入力した場合、AIは以下のルールに従うこと。
- ベース案（A/B/C）の制約を完全に無視する。
- 人間のテキスト指示を **100%** 再現することを最優先とする。
- 指示に曖昧な点がある場合は、コード生成前に必ず追加質問を行い仕様を確定させる。自己解釈で進めることを禁ずる。

### STEP 5: 1セクションごとの確定ルール
1つのセクションのコーディングが完了したら、AIは必ずそこで処理を止め、人間にブラウザでのプレビュー確認を促すこと。
人間から「OK」「次へ」などの**明示的な承認を受け取るまで、絶対に次のセクションのコーディングを開始してはならない。**

---

## 3. 運用ルールの分離について

- 人間側の運用フロー、Googleドライブ連携、フォルダ命名規則などについては、人間向けマニュアルである `system-requirements.md` を参照すること（AIがこれを読んで自律行動する必要はない）。
- AIは、`our-standard-rules.md` に記載されたコーディングルールを、上記の対話フローの中で100%忠実に実行する「実務担当者」として振る舞うこと。
