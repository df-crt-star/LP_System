# CLAUDE.md — LP_System プロジェクト指示書

このファイルはClaude Codeがこのリポジトリで作業する際の指示書です。

---

## Coding Guidelines（HTML/CSS/JS 絶対遵守ルール）

**本プロジェクトでHTML・CSS・JavaScriptを生成・編集する際は、必ず `/our-standard-rules.md`（制作部共通コーディングルール・ガイドライン）をルールブックとして厳格に遵守すること。一項目たりとも省略・無視することを禁ずる。**

主要な遵守事項（詳細は `our-standard-rules.md` を参照）:

- **HTML構造**: `lang="ja"`, `charset="UTF-8"`, セマンティックタグ（`<section>` / `<main>` / `<header>` / `<footer>`）、`<h1>` はページ内1回のみ、見出し階層を正しく維持すること
- **ファイル構成**: `index.html` / `css/style.css` / `js/main.js` / `images/` / `videos/` の標準構成を厳守すること
- **CSSリセット**: すべてのCSSファイル冒頭に `our-standard-rules.md` §3 のリセット・ベーススタイルブロックを必ず記述すること
- **レスポンシブ**: モバイルファースト・`min-width` メディアクエリのみ使用（`max-width` のみは禁止）。ブレークポイントは sm(〜767px) / md(768px〜) / lg(1200px〜)
- **画像**: WebP形式、`<img>` タグに `width` / `height` / `alt`（空文字禁止）必須。ファーストビュー外は `loading="lazy"`
- **動画**: MP4は `autoplay muted playsinline loop` の4属性すべて必須
- **クラス命名**: ケバブケース（kebab-case）のみ。キャメル・スネーク・パスカルは禁止
- **JavaScript**: Vanilla JSのみ（jQuery等の外部ライブラリ禁止）、`var` 禁止、スクロールアニメーションは `IntersectionObserver` を使用
- **絶対禁止**: `!important` の多用、インラインスタイル直書き、`<table>` レイアウト、`background-image` による非装飾画像の埋め込み、`scroll` イベントへの直接バインド

コード生成前に `our-standard-rules.md` の該当セクションを参照し、すべての要件を満たしていることを確認してからコードを出力すること。

---

## AIの厳格な行動制限（対話型カスタムビルドフロー）

**以下の行動制限は、Coding Guidelinesと同等の最優先ルールである。コーディング作業の開始前・進行中・完了後のすべての局面において必ず遵守すること。**

### 制限1: 起動時ヒアリングなしのコーディング開始は絶対禁止

操作者からコーディング・LP制作の依頼を受けた際、AIは即座にコードを書き始めてはならない。必ず最初に以下の4項目をヒアリングし、すべての回答が揃うまで一行もコードを出力しないこと。

```
【初期ヒアリング】
以下の4項目をすべてご回答ください。確認が取れ次第、コーディングを開始します。

1. PC版の最大横幅（例：1200px / 1000px）
2. 全体の基本フォント・メインカラー・アクセントカラー
   （例：Noto Sans JP / メイン #333333 / アクセント #e74c3c）
3. 使用予定の外部ライブラリ
   （例：AOS.js, Swiper.js ／ なければ「なし」）
4. その他、全体を通して適用したい独自ルール（自由記述。なければ「なし」）
```

### 制限2: ヒアリング後は必ず素材スキャンを実施すること

初期ヒアリングの回答が揃い次第、AIは案件フォルダ内の `images/`・`videos/` 等を自動スキャンし、格納されているファイル（WebP・MP4・GIF）の一覧を操作者に提示すること。スキャンを省略してコーディングに入ることを禁止する。

### 制限3: セクション制作前の対話テンプレート出力は必須

各セクションのコーディングに入る前に、AIは必ず以下の形式で操作者へ確認を行うこと。この確認をスキップして勝手にコーディングを開始することを禁止する。

```
-----------------------------------------
次は「[セクション名]」の制作です。
以下の選択肢からベース仕様をお選びいただくか、[4]で自由にカスタム指示をしてください。

[1] フォーマットA：[セクションに適した構成①]
[2] フォーマットB：[セクションに適した構成②]
[3] フォーマットC：[セクションに適した構成③]
[4] 自由カスタム（テキスト入力）：
    一部変更や完全オリジナルの仕様を言葉で自由に指示してください。
-----------------------------------------
```

### 制限4: 自由カスタム指示（選択肢4）は100%最優先で再現すること

操作者が [4] を選択、またはテキストで直接要望を記述した場合、AIは提示した固定フォーマット（A/B/C）に縛られることなく、人間の指示を完全に再現するコードを生成すること。指示に曖昧な点がある場合は、コード生成前に必ず追加質問を行い仕様を確定すること。追加質問なしに「たぶんこういう意味だろう」と解釈してコードを書くことを禁止する。

### 制限5: 1セクション完了後は必ず処理を停止し確認を待つこと

1セクションのコーディングが完了したら、AIは必ずそこで処理を止め、操作者にブラウザでのプレビュー確認を促すメッセージを出力すること。操作者から明示的に「OK」「次へ」「次のセクションへ」等の合図が届くまで、次のセクションのコーディングを絶対に開始してはならない。
