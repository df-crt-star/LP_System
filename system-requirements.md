# LP自動コーディングシステム 要件定義書（system-requirements.md）

## 1. システム概要
本システムは、制作部におけるLP（ランディングページ）制作のコーディング工程をAI（Claude Code）によって完全自動化・高速化し、属人化の解消と制作期間の圧倒的な短縮を実現するための自律型開発システムである。

## 2. 導入背景と目的
- **現状の課題**: コーダーごとの実装方法のばらつき、レスポンシブ対応の手間、各種ECカート・LPツールへの移植時の表示崩れ。
- **導入目的**: 
  1. 社内共通の品質基準を100%満たしたソースコードの高速自動生成。
  2. プラットフォーム（Shopify, EC Force等）へのコンバートを前提とした、プレーンで美しいマークアップの徹底。

## 3. ディレクトリ設計
lp-system/ (プロジェクトルート)
├── CLAUDE.md              # AIの行動指針・絶対遵守ルールの定義
├── system-requirements.md # システム全体の要件定義書（本ファイル）
├── our-standard-rules.md  # 制作部共通コーディングルール・ガイドライン
└── 01-test/               # 各案件・テスト用の作業フォルダ（AIが自動生成）


## 4. 人間側の運用フロー（Googleドライブ）
非エンジニアのメンバーでも直感的に素材の受け渡しができるよう、Google Workspace上の共有ドライブを活用する。
1. **📥 📥 【01_案件投入（人間 → AI）】**: デザイナーやディレクターが新規案件フォルダ（例: 01_サプリメントLP）を共有ドライブ内に作成し、デザインカンプやWebP素材を格納する。AI（Manus等）はこのフォルダのURLから素材を読み込む。
2. **🤖 【02_作業スペース（AI専用）】**: 本リポジトリ（lp-system）内。AIがコードを書く専用工場とし、人間は原則手動での書き換えを行わない。
3. **📤 【03_納品確認（AI → 人間）】**: AIはコーディング完了後、成果物（index.html、css、js）をZip等に固めて共有ドライブの「03_納品確認」フォルダに自動アップロードする。人間はこれをダウンロードしてブラウザで表示チェックを行う。

## 5. 外部連携（Google Chat通知）
AI（Claude Code / Manus）は、上記「03_納品確認」へのファイルアップロードが完了した成否をトリガーとして、制作部のGoogle Chatの専用スペースへWebhook経由で以下の内容を自動通知すること。
- 案件名、作業フォルダ名
- 納品先（Googleドライブ）のURL
- 生成されたファイル一覧

## 6. 複数人運用における注意点（制約事項）
- **アカウント**: 制作部共通のチームプランアカウント（df-crt@diamond-f.co.jp）を想定利用。
- **競合の防止**: システム利用者（3名）が同時に同一アカウントでClaude Codeを起動すると、AIのセッションやGitブランチが競合し上書きバグが発生するため、同時並行での作業を原則禁止とし、案件ごとに作業時間をコントロールする。
- **権限の徹底**: Googleドライブの親フォルダは、ドメイン内（@diamond-f.co.jp）の利用メンバーおよび共通アカウントに対して「編集者権限」を事前に完全付与すること。

## 7. 機能要件（AIに実装させる基本ルール）
1. **基本テックスタック**: HTML5、CSS3、Vanilla JS（外部ライブラリ禁止）。
2. **素材の最適化**: 画像形式はWebPを標準とし、<img>タグにはwidth, height, alt属性を完全必須とする。
3. **動画 (MP4) の自動再生**: <video>タグにはautoplay muted playsinline loopの4属性を必ず同時付与する。
4. **命名規則**: CSSクラス名はすべてケバブケース（例: foo-bar）で統一。簡易BEM設計を採用。
5. **頻出演出**: 背景画像の上に要素を絶対配置（position: absolute）で重ね、汎用クラス .animation-float で上下にふわふわループ可動させる。
6. **移植性の担保**: EC Force、Shopify、WordPress等への移植・インラインCSS化を見据え、ハック的なコードを排除し、美しく標準的な構造を徹底する。

## 8. 運用の絶対禁止事項
- !important の多用
- HTMLへのインラインスタイル（style=""）の直接記述
- <table> タグによるレイアウト
- scroll イベントへの直接バインド（IntersectionObserverを使用すること）
