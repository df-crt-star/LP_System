# LP自動コーディング化システム 運用マニュアル

> **【注意】**
> 本ドキュメントは**人間（制作メンバー）向けの運用マニュアル**です。
> AI（Claude Code）は本ファイルの内容を読み込んで自律行動する必要はありません。

---

## 1. システムの目的と全体像

本システムは、Claude Code等のデスクトップ版AIエディタを利用し、厳格なコーディングルールに則ったLP（ランディングページ）を半自動で構築するためのフレームワークです。

「AIが勝手にコードを書き進めて破綻する」ことを防ぐため、**「対話型カスタムビルドフロー（1セクションごとの確認）」**を採用しています。

---

## 2. システムフォルダ構成

```
LP_System/
├── CLAUDE.md                  # 指揮官ファイル（AIへの行動指示書）
├── system-requirements.md     # 本ファイル（人間向け運用マニュアル）
├── core/
│   ├── rules-core.md          # 絶対ルール（128行・CSS変数定義・禁止事項）
│   └── rules-reference.md     # コード例置き場（AIが必要時のみ参照）
└── components/
    ├── animation.md            # アニメーション実装テンプレート（A1〜A8）
    ├── section-templates.md    # セクションレイアウトテンプレート（A/B/C案）
    ├── form.md                 # フォーム・アコーディオンCTA
    ├── sticky.md               # 追従CTAバー
    ├── voice.md                # ユーザーボイス・お客様の声
    └── faq.md                  # アコーディオンFAQ
```

**廃止済み：** `profiles/` フォルダ（ジャンルプロファイル・トーンファイル）、`our-standard-rules.md`（`core/rules-core.md` に移行）

---

## 3. 案件フォルダ構成

案件ごとにプロジェクトルート直下にフォルダを作成します。

```
案件フォルダ（例: 20260618-sample-corp/）
├── index.html
├── css/
│   └── style.css
├── js/
│   └── main.js
├── images/          # コーディング用素材（WebP・GIF）
├── videos/          # コーディング用動画（MP4）
└── design/          # デザインカンプ置き場（★ 下記参照）
    ├── 01_fv.jpg
    ├── 02_features.jpg
    └── 03_flow.jpg
```

### `design/` フォルダ 運用ルール

`design/` フォルダはAIがコーディング精度を上げるためにデザインカンプを格納する専用フォルダです。

**ファイル命名規則：**

```
[2桁連番]_[セクション名].jpg
```

| 例 | セクション |
|----|----------|
| `01_fv.jpg` | ファーストビュー（FV）|
| `02_features.jpg` | 特徴・選ばれる理由 |
| `03_flow.jpg` | ご利用の流れ |
| `04_voice.jpg` | お客様の声 |
| `05_faq.jpg` | よくある質問 |
| `06_form.jpg` | フォーム・CTA |

**運用ルール：**

1. カンプはセクション単位で切り出してJPG保存する（1ファイル = 1セクション）
2. 連番は画面上から順に振る（`01_` がページ最上部）
3. ファイル名はすべて**小文字のケバブケース**（例: `01_fv.jpg`、`04_user-voice.jpg`）
4. カンプが存在するセクションはAIが自動参照してコーディングする（STEP 0.5 / STEP 3）
5. カンプが存在しないセクションはAIがヒアリングで仕様を確認する
6. 解像度は幅 **1000px 以上**推奨（AIの視認精度向上のため）
7. `design/` フォルダは納品物に含めない（Zip化の際は除外すること）

---

## 4. チーム開発・ファイル管理ルール

制作メンバー3名で作業を分担し、コンフリクトを防ぐためのルールです。

### 4.1 バージョン管理（Git / SourceTree）

- 本システム（ルールファイルやコンポーネント定義）の変更は、Gitでバージョン管理し、SourceTreeを用いて共有します
- 案件のコード自体（HTML/CSS等）はGit管理の対象外とし、以下のローカル完結型フローで管理します

### 4.2 案件フォルダの命名規則と管理

- 案件ごとにプロジェクトルート直下にフォルダを作成します
- フォルダ命名規則: `YYYYMMDD-client-name`（例: `20260618-sample-corp`）
- 複数のメンバーが同時に別案件を作業する場合は、必ずこの命名規則に従い、フォルダ名の衝突を防ぎます
- 同一アカウントでClaude Codeを複数同時に起動すると競合が発生するため、案件ごとに作業時間をコントロールしてください

### 4.3 納品・ファイル共有フロー（Google Drive）

- 案件が完了したら、案件フォルダをZip化します（例: `20260618-sample-corp.zip`）
- **`design/` フォルダは納品物に含めないこと**（Zip化の際は除外する）
- 完成したZipファイルのみを、共有のGoogleドライブ（03_納品確認 フォルダ）にアップロードします
- 進行中の生ファイル（HTML/CSS等）はGoogleドライブで同期せず、各担当者のローカル環境で完結させます

---

## 5. ルールファイルの追加・修正（メンテナンス）

AIの挙動を変更したり、新しいアニメーションやフォームのレイアウトを追加したりする場合は、以下のルールに従ってファイルを編集してください。

| ファイル | 編集タイミング |
|---------|--------------|
| `CLAUDE.md` | AIの対話フロー（ステップの順序・質問の仕方）を変更したい場合のみ |
| `core/rules-core.md` | CSS変数のデフォルト値・禁止事項・グローバルルールを変更したい場合 |
| `core/rules-reference.md` | コード例（HTML/CSS/JS）を追加・修正したい場合 |
| `components/animation.md` | アニメーションの種類を追加・修正したい場合 |
| `components/section-templates.md` | セクションレイアウトのA/B/C案を追加・修正したい場合 |
| `components/form.md` | フォーム・CTAのテンプレートを追加・修正したい場合 |
| `components/sticky.md` | 追従バーのテンプレートを追加・修正したい場合 |
| `components/voice.md` | ユーザーボイスのテンプレートを追加・修正したい場合 |
| `components/faq.md` | FAQのテンプレートを追加・修正したい場合 |

編集後は、必ずGitでコミット・プッシュし、他のメンバーに共有してください。
