# LP自動コーディング化システム 運用マニュアル

> **【注意】**
> 本ドキュメントは**人間（制作メンバー）向けの運用マニュアル**です。
> AI（Claude Code）は本ファイルの内容を読み込んで自律行動する必要はありません。

---

## 1. システムの目的と全体像

本システムは、Claude Code等のデスクトップ版AIエディタを利用し、厳格なコーディングルールに則ったLP（ランディングページ）を半自動で構築するためのフレームワークです。

「AIが勝手にコードを書き進めて破綻する」ことを防ぐため、**「対話型カスタムビルドフロー（1セクションごとの確認）」**を採用しています。

## 2. チーム開発・ファイル管理ルール

制作メンバー3名で作業を分担し、コンフリクトを防ぐためのルールです。

### 2.1 バージョン管理（Git / SourceTree）
- 本システム（ルールファイルやコンポーネント定義）の変更は、Gitでバージョン管理し、SourceTreeを用いて共有します。
- 案件のコード自体（HTML/CSS等）はGit管理の対象外とし、以下のローカル完結型フローで管理します。

### 2.2 案件フォルダの命名規則と管理
- 案件ごとにプロジェクトルート直下にフォルダを作成します。
- フォルダ命名規則: `YYYYMMDD-client-name` （例: `20260618-sample-corp`）
- 複数のメンバーが同時に別案件を作業する場合は、必ずこの命名規則に従い、フォルダ名の衝突を防ぎます。
- 同一アカウントでClaude Codeを複数同時に起動すると競合が発生するため、案件ごとに作業時間をコントロールしてください。

### 2.3 納品・ファイル共有フロー（Google Drive）
- 案件が完了したら、案件フォルダをZip化します（例: `20260618-sample-corp.zip`）。
- 完成したZipファイルのみを、共有のGoogleドライブ（03_納品確認 フォルダ）にアップロードします。
- 進行中の生ファイル（HTML/CSS等）はGoogleドライブで同期せず、各担当者のローカル環境で完結させます。

## 3. ルールファイルの追加・修正（メンテナンス）

AIの挙動を変更したり、新しいアニメーションやフォームのレイアウトを追加したりする場合は、以下のルールに従ってファイルを編集してください。

1. **`CLAUDE.md`**
   - AIへの行動指示書です。AIの対話フロー（質問の仕方など）を変更したい場合のみ編集します。
2. **`our-standard-rules.md`**
   - 全体に適用されるベースルール（CSSリセット、禁止事項など）です。
3. **`components/*.md`**
   - **最も頻繁に編集するフォルダです。**
   - 新しいアニメーションを追加したい場合は `components/animation.md` を編集・作成します。
   - 新しいセクションレイアウト（A/B/C案）を追加したい場合は `components/section-templates.md` を編集・作成します。

編集後は、必ずGitでコミット・プッシュし、他のメンバーに共有してください。
