# LP自動コーディングシステム 要件定義書（system-requirements.md）

## 1. システム概要
本システムは、制作部におけるLP（ランディングページ）制作のコーディング工程をAI（Claude Code）によって完全自動化・高速化し、属人化の解消と制作期間の圧倒的な短縮を実現するための自律型開発システムである。

## 2. 導入背景と目的
- **現状の課題**: コーダーごとの実装方法のばらつき（命名規則、入れ子構造の乱れ）、レスポンシブ対応の手間、各種ECカート・LPツールへの移植時の表示崩れ。
- **導入目的**: 
  1. 社内共通の品質基準を100%満たしたソースコードの高速自動生成。
  2. プラットフォーム（Shopify, EC Force等）へのコンバートを前提とした、プレーンで美しいマークアップの徹底。

## 3. ディレクトリ設計
プロジェクトルート直下に共通の要件定義とルールファイルを常駐させ、案件ごとに「連番フォルダ」を作成して独立した開発環境を作る。

lp-system/ (プロジェクトルート)
├── CLAUDE.md              # AIの行動指針・基本コマンド・絶対遵守ルールの定義
├── system-requirements.md # システム全体の要件定義書（本ファイル）
├── our-standard-rules.md  # 制作部共通コーディングルール・ガイドライン
└── 01-test/               # 各案件・テスト用の作業フォルダ（AIが自動生成）

## 4. 機能要件（AIに実装させる基本ルール）
1. **基本テックスタックの固定**: HTML5、CSS3、Vanilla JS（外部ライブラリへの依存は一切禁止）。
2. **素材の最適化**: 画像形式は WebP (`.webp`) を標準とし、`<img>` タグには `width`, `height`, `alt` 属性を完全必須とする。
3. **動画 (MP4) の扱い**: スマホ・ブラウザでの自動再生を確実にするため、`<video>` タグには `autoplay muted playsinline loop` の4属性を必ず同時付与する。
4. **命名規則と設計**: CSSクラス名はすべてケバブケース（例: `foo-bar`）で統一。破綻しにくい「簡易BEM設計」を採用する。
5. **頻出演出のテンプレート化**: 背景画像の上に要素を絶対配置で重ね、汎用クラス `.animation-float` で上下にふわふわループ可動させる。
6. **移植性（汎用性）の担保**: 「EC Force」「EC-CUBE」「SquadBeyond」「Shopify」「WordPress」への移植・インラインCSS化を見据え、ハック的なコードを排除し、美しく標準的な構造を徹底する。

## 5. 運用の絶対禁止事項
本システムがコードを生成する際、以下の実装はバグや保守性低下に繋がるため完全禁止とする。
- `!important` の多用
- HTMLへのインラインスタイル（`style=""`）の直接記述
- `<table>` タグによるレイアウト
- `scroll` イベントへの直接バインド（スクロール連動は `IntersectionObserver` を使用する）
- `<img>` タグの各種属性（width, height, alt）の省略
